<div class="max-w-xl mx-auto py-10 px-4">
  <div class="mb-6">
    <%= link_to @project.name, @project, class: "text-sm text-gray-500 hover:text-gray-800" %>
  </div>
  <h1 class="text-2xl font-semibold mb-6">Edit project</h1>
  <%= render "form", project: @project %>
</div>
